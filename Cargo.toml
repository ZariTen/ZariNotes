[package]
name = "zarinotes"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[dependencies]
iced = { version = "0.14", features = ["tokio", "svg"] }
rfd = "0.17"

[profile.release]
strip = true
lto = "thin"
